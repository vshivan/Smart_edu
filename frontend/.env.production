# This file is committed — it only contains the public API URL, no secrets.
# After deploying to Render, replace the value below with your actual Render URL
# then redeploy Vercel (or set VITE_API_URL in Vercel dashboard instead).
VITE_API_URL=https://smartedulear-server.onrender.com
